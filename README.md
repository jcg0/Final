# Final Project Prototype

This repository now contains a Unity prototype scaffold in `UnityPrototype/` for a hexagonal turn-based strategy game part-1 implementation.

See `UnityPrototype/README.md` for setup instructions, controls, included assets, and gameplay rule constraints.
